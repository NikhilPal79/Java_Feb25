package MAR11.ENCAPSULATION;

public class READ {

    /*
    wraps the data into single unit... programmer can control who can access what

    private ?

    by default you should make all the fields/data members as private no one can access it outside.

    Parent().display() Child(). main()

    */
}
