package InterviewQuestions;

public class Set {


    /// SET ===> A Set is a collection that

    /// Data Structure
    // 1. stores unique values
    // 2. does not have order
    // 3. can have null but just.


/// SET

/// 1. HashSet ==========> RANDOM ORDER, ALLOW THE NULL ELEMENT.
/// 2. TreeSet ==========> NATURAL SORTING ORDER NO NULL IS ALLOWED
/// 3. LinkedHashSet ====> ORDER IS MAINTAINED INSERTION ORDER


    /// When to Use a Set?
    /// When you want to avoid duplicates
    ///
    /// When order doesn’t matter (HashSet)
    ///
    /// When you need to maintain insertion order (LinkedHashSet)
    ///
    /// When you need sorted output (TreeSet)
}
